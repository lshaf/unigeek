[env:m5_cores3]
board = m5_cores3
board_build.partitions = firmware/partitions_16mb.csv

build_src_filter = ${env.build_src_filter} +<../boards/m5_cores3>
build_flags =
	${env.build_flags}
	-Ifirmware/boards/m5_cores3

lib_deps =
	${env.lib_deps}